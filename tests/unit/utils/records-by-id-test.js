import recordsById from 'contributors-days/utils/records-by-id';
import { module, test } from 'qunit';

module('Unit | Utility | records by id');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = recordsById();
  assert.ok(result);
});
